// SeeAlso: 

#include <bits/stdc++.h>

#define MAX 2000
#define rep(i,n) for(int i = 0; i < n; i++)

using namespace std;

int N, M;
int S[MAX], T[MAX];

int main() {
    
    cin >> N >> M;

    rep(i, N) {
        cin >> S[i];
    }

    rep(i, M) {
        cin >> T[i];
    }

    
    return 0;
}
